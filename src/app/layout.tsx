import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LaunchPad - Multi-Agent Startup Validation Platform',
  description: 'Validate your startup idea with AI-powered market research, MVP architecture, and competitive analysis.',
  keywords: ['startup', 'validation', 'AI', 'market research', 'MVP', 'competitive analysis'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
